import React from "react";
import styled from "styled-components";

const Signature = styled.div`
  font-family: "Reenie Beanie", cursive;
  font-size: 30px;
  margin-left: 30px;
`;
const Logo = () => <Signature>Blake</Signature>;

export default Logo;
